# 滚动条

- [网易组件库说明文档](http://webpack.nie.netease.com/index.html#pack=pc_outline/scroll/nicescroll.html)
- [jquery.nicescroll：github](https://github.com/inuyaksa/jquery.nicescroll)

```js
$(document).ready(function () {
    $("html").niceScroll({
        cursorcolor: "#000",// 十六进制改变光标颜色，默认值是“＃000000”
        cursoropacitymin: 0,// 改变不透明度非常光标处于非活动状态（scrollabar“隐藏”状态），范围从1到0，默认为0（隐藏）
        cursoropacitymax: 1,// 改变不透明度非常光标处于活动状态（scrollabar“可见”状态），范围从1到0，默认值是1（完全不透明）
        cursorwidth: 5,// 像素光标的宽度，默认值为5（你可以写“加入5px”太）
        cursorborder: "1px solid ＃FFF",// 游标边框css定义，默认为'1px solid ＃FFF'
        cursorborderradius: "5px",// 边界圆角半径，默认为'5px'
        zIndex: "9999",// 改变z-index值的滚动条的div，默认值是9999
        scrollspeed: 0,// 滚动速度，默认值为60
        mousescrollstep: 40,// 高速滚动鼠标滚轮，默认值是40（像素）
        touchbehavior: false,// 使光标拖动滚动像在台式电脑触摸设备（默认：false）
        hwacceleration: true,// 使用硬件加速滚动支持的时候（默认：true）
        boxzoom: false,// 使变焦框中的内容（默认：false）
        dblclickzoom: true,// （仅当boxzoom = TRUE）变焦激活时，双击对话框（默认：true）
        gesturezoom: true,// （仅当boxzoom =真实，使用触摸设备）上缩放框激活时，间距输出/输入（默认：true）
        grabcursorenabled: true,//显示“抢”图标的div touchbehavior = true时，（默认：true）
        autohidemode: true,//如何隐藏滚动条的作品，true=默认/“光标”=只进游标隐藏/ false =不隐藏 背景，CSS改变轨道的背景下，默认为“” 在加载事件iframeautoresize**，AUTORESIZE的iframe（默认：true）
        cursorminheight: 20,//设置在像素的最小光标高度（默认值：20）
        preservenativescrolling: true,//您可以滚动本机可滚动区域用鼠标，冒泡鼠标滚轮事件（默认：true）
        railoffset: false,//您可以添加抵消顶部/左边的轨道位置（默认：false）
        bouncescroll: false,//使滚动弹跳在内容结尾作为移动像（仅HW ACCELL）（默认：false）
        spacebarenabled: true,//使向下翻页时，空格键已经按下滚动（默认：true）
        railpadding: "0 0 0 0",//设置填充为轨道式（默认值：{顶：0，右：0，左：0，下：0}）
        disableoutline: true,//对于chrome浏览器，停用大纲（橙色hightlight）选择具有nicescroll一个div（默认：true）时
        horizrailenabled: true,//nicescroll可以管理水平滚动（默认：true）
        railalign: "right",//取向垂直导轨（defaul：“右”）
        railvalign: "bottom",//对齐水平导轨（defaul：“底部”）
        enabletranslate3d: true,//nicescroll可以使用CSS转换为滚动内容（默认：true）
        enablemousewheel: true,//nicescroll可以管理的鼠标滚轮事件（默认：true）
        enablekeyboard: true,//nicescroll可以管理键盘事件（默认：true）
        smoothscroll: true,//滚动自如移动（默认：true）
        sensitiverail: true,//点击轨道上进行滚动（默认：true）
        enablemouselockapi: false,//可以用鼠标说明锁的API（对象拖动同样的问题）（默认：true） 用于光标在像素cursorfixedheight，设置固定的高度（默认：false）
        hidecursordelay: 6,//设置在微秒淡出滚动条的延迟时间（默认值：400） 在对方向锁定激活像素directionlockdeadzone，死区（默认值：6）
        nativeparentscrolling: true,//检测内容底部，并让家长来滚动，作为原生滚动做（默认：true）
        enablescrollonselection: true,//启用自动滚动的内容时，选择文本（默认：true）
    });
});
```


```js
$("#thisdiv").niceScroll({
    cursorcolor: "#424242", // change cursor color in hex
    cursoropacitymin: 0, // change opacity when cursor is inactive (scrollabar "hidden" state), range from 1 to 0
    cursoropacitymax: 1, // change opacity when cursor is active (scrollabar "visible" state), range from 1 to 0
    cursorwidth: "5px", // cursor width in pixel (you can also write "5px")
    cursorborder: "1px solid #fff", // css definition for cursor border
    cursorborderradius: "5px", // border radius in pixel for cursor
    zindex: "auto" | [number], // change z-index for scrollbar div
    scrollspeed: 60, // scrolling speed
    mousescrollstep: 40, // scrolling speed with mouse wheel (pixel)
    touchbehavior: false, // DEPRECATED!! use "emulatetouch"
    emulatetouch: false, // enable cursor-drag scrolling like touch devices in desktop computer
    hwacceleration: true, // use hardware accelerated scroll when supported
    boxzoom: false, // enable zoom for box content
    dblclickzoom: true, // (only when boxzoom=true) zoom activated when double click on box
    gesturezoom: true, // (only when boxzoom=true and with touch devices) zoom activated when pinch out/in on box
    grabcursorenabled: true, // (only when touchbehavior=true) display "grab" icon
    autohidemode: true, // how hide the scrollbar works, possible values: 
                        /*
                          true | // hide when no scrolling
                          "cursor" | // only cursor hidden
                          false | // do not hide,
                          "leave" | // hide only if pointer leaves content
                          "hidden" | // hide always
                          "scroll", // show only on scroll  
                        * */
    background: "", // change css for rail background
    iframeautoresize: true, // autoresize iframe on load event
    cursorminheight: 32, // set the minimum cursor height (pixel)
    preservenativescrolling: true, // you can scroll native scrollable areas with mouse, bubbling mouse wheel event
    railoffset: false, // you can add offset top/left for rail position
    bouncescroll: false, // (only hw accell) enable scroll bouncing at the end of content as mobile-like 
    spacebarenabled: true, // enable page down scrolling when space bar has pressed
    railpadding: { top: 0, right: 0, left: 0, bottom: 0 }, // set padding for rail bar
    disableoutline: true, // for chrome browser, disable outline (orange highlight) when selecting a div with nicescroll
    horizrailenabled: true, // nicescroll can manage horizontal scroll
    railalign: right, // alignment of vertical rail
    railvalign: bottom, // alignment of horizontal rail
    enabletranslate3d: true, // nicescroll can use css translate to scroll content
    enablemousewheel: true, // nicescroll can manage mouse wheel events
    enablekeyboard: true, // nicescroll can manage keyboard events
    smoothscroll: true, // scroll with ease movement
    sensitiverail: true, // click on rail make a scroll
    enablemouselockapi: true, // can use mouse caption lock API (same issue on object dragging)
    cursorfixedheight: false, // set fixed height for cursor in pixel
    hidecursordelay: 400, // set the delay in microseconds to fading out scrollbars
    directionlockdeadzone: 6, // dead zone in pixels for direction lock activation
    nativeparentscrolling: true, // detect bottom of content and let parent to scroll, as native scroll does
    enablescrollonselection: true, // enable auto-scrolling of content when selection text
    cursordragspeed: 0.3, // speed of selection when dragged with cursor
    rtlmode: "auto", // horizontal div scrolling starts at left side
    cursordragontouch: false, // drag cursor in touch / touchbehavior mode also
    oneaxismousemode: "auto", // it permits horizontal scrolling with mousewheel on horizontal only content, if false (vertical-only) mousewheel don't scroll horizontally, if value is auto detects two-axis mouse
    scriptpath: "", // define custom path for boxmode icons ("" => same script path)
    preventmultitouchscrolling: true, // prevent scrolling on multitouch events
    disablemutationobserver: false, // force MutationObserver disabled,
    enableobserver: true, // enable DOM changing observer, it tries to resize/hide/show when parent or content div had changed
    scrollbarid: false // set a custom ID for nicescroll bars 
});
```


```js

$("#thisdiv").niceScroll({

    Cursorcolor: "#424242", //以十六进制改变光标颜色
    Cursoropacitymin: 0, //当光标处于非活动状态时改变不透明度(滚动条“隐藏”状态),范围从1到0
    Cursoropacitymax: 1, //当光标处于活动状态(滚动条“可见”状态)时改变不透明度,范围从1到0
    Cursorwidth: "5px", //以像素为单位的光标宽度(你也可以写"5px")
    Cursorborder: "1px solid #fff", // CSS定义光标边框
    Cursorborderradius: "5px", //以像素为单位的光标边界半径
    Zindex: "auto" | [number], //改变滚动条的z-index
    Scrollspeed: 60, //滚动速度
//鼠标滚轮的滚动速度(像素)
    touchbehavior: false, //已弃用!使用“emulatetouch”
    Emulatetouch: false, //启用像桌面电脑中的触摸设备一样的鼠标拖动滚动
    Hwacceleration: true, //支持时使用硬件加速滚动
    Boxzoom: false, //为盒子内容启用缩放
    Dblclickzoom: true, //(仅当boxzoom=true时)当双击盒子时缩放被激活
    Gesturezoom: true, //(仅当boxzoom=true和触摸设备)缩放激活时捏出/在盒子上
    Grabcursorenabled: true, //(仅当touchbehavior=true时)显示“抓取”图标
    Autohidemode: true, //如何隐藏滚动条,可能的值:
    /*
    True | //当没有滚动时隐藏
    "cursor" | //只隐藏游标
    False | //不隐藏
    "leave" | //只在指针离开内容时隐藏
    "hidden" | // hide always
    "scroll", //只显示在滚动条上
    **/
    Background: "", //改变铁路背景的CSS
    iframeautoresize: true, //自动调整iframe加载事件的大小
    Cursorminheight: 32, //设置光标的最小高度(像素)
    preservenativesrolling: true, //你可以用鼠标滚动本地可滚动区域,冒泡鼠标滚轮事件
    Railoffset: false, //你可以为轨道位置添加顶部/左侧偏移量
    bouncescroroll: false, //(只有hw accell)允许滚动在内容结束时像移动设备一样弹跳
    Spacebarenabled: true, //当按下空格键时启用页面向下滚动
    Railpadding: {top: 0, right: 0, left: 0, bottom: 0}, //设置轨道栏的padding
    Disableoutline: true, //对于chrome浏览器,当选择带有nicesscroll的div时禁用outline(橙色高亮)
// nicesscroll可以管理水平滚动
    Railalign: 右对齐,//垂直轨道对齐
    轨道对齐: 底部,//水平轨道对齐
    Enabletranslate3d: true, // nicesscroll可以使用CSS翻译来滚动内容
    Enablemousewheel: true, // nicesscroll可以管理鼠标滚轮事件
    Enablekeyboard: true, // nicesscroll可以管理键盘事件
    Smoothscroll: true, //轻松滚动
    Sensitiverail: true, //点击轨道产生滚动
    enablemouselockapi: true, //可以使用鼠标标题锁定API(拖动对象时也是如此)
    Cursorfixedheight: false, //以像素为单位设置光标的固定高度
    Hidecursordelay: 400, //设置滚动条淡出的延迟(以微秒为单位)
    Directionlockdeadzone: 6, //方向锁激活的死区像素
    Nativeparentscrolling: true, //检测内容的底部并让parent滚动,就像native scroll一样
    Enablescrollonselection: true, //在选择文本时启用自动滚动内容
    Cursordragspeed: 0.3, //用光标拖动时的选择速度
    Rtlmode: "auto", //水平div滚动从左侧开始
    Cursordragontouch: false, //在touch / touchbehavior模式下拖动光标
    Oneaxismousemode: "auto", //它允许在水平内容上用鼠标滚轮水平滚动,如果false(仅垂直)鼠标滚轮不水平滚动,如果值为auto则检测两轴鼠标
    Scriptpath: "", //为boxmode图标定义自定义路径("" =>相同的脚本路径)
    Preventmultitouchscrolling: true, //阻止多点触控事件的滚动
    disablemutationobserver: false, //强制禁用MutationObserver;
    enableobserver: true, //启用DOM改变观察者,当父div或内容div改变时,它会尝试调整/隐藏/显示
    scrollbarid: false //为滚动条设置自定义ID****
})

```


以下是niceScroll插件常用的回调函数：

- onScrollStart：当滚动开始时触发。
- onScrollEnd：当滚动结束时触发。
- onScroll：当滚动时触发，可以获取到当前滚动的位置。
- onResize：当窗口大小改变时触发，可以进行一些布局调整。
- onTouchStart：当触摸屏幕开始时触发。
- onTouchEnd：当触摸屏幕结束时触发。
- onOverflowChanged：当内容溢出时触发。
- onOverflowY：当垂直内容溢出时触发。
- onOverflowX：当水平内容溢出时触发。


有冲突报错

<p style="color: red">jquery.nicescroll.3.6.8.js:14 [Intervention] Unable to preventDefault inside passive event listener due to target being treated as passive. See https://www.chromestatus.com/feature/6662647093133312</p>

```js
// 源码示例：
dom.addEventListener('scroll',mousewheel,{passive:true});

// 修改后的示例：
dom.addEventListener('scroll',mousewheel,{passive:false});
```

或者用最新版本：jquery.nicescroll.3.7.6.js